"""
Chapter 0 — FULL ASSEMBLY (not built yet)

After each act file is approved:
  scene0_act1.py  ✅ in progress
  scene0_act2.py  ← next
  scene0_act3.py
  scene0_act4.py
  scene0_act5.py
  scene0_act6.py

Then import each act's motion here and concat into one Scene class.
Do not build this file until acts 1–6 pass layout + motion review.
"""

# from scene0_act1 import ...  (example — wire when act 1 locked)
